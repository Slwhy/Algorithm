1,ArrayList如何遍历
    for each循环
2，hashmap内的值如何进行修改
    HashMap<String,Integer> map = new HashMap<String,Integer>();
    Integer value = map.get(String.valueOf(key));
    value++;
    map.put(String.valueOf(key),value);
3,sort方法如何使用见 最小的K个数

4.java 中字符串相关函数
Character.isDigit(char)     判断是否是数字
Character.isLetter(char)    判断是否是字母
Character.toLowerCase(char) 字符大写转小写

5.一个char[] (字符数组),调用 toString 函数返回的不是字符数组对应的字符串,而是对象的名称

